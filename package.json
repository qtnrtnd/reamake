{
  "name": "scripts",
  "version": "1.0.0",
  "description": "",
  "main": "./bin/index.js",
  "type": "module",
  "scripts": {
    "build-cjs": "esbuild ./bin/index.js --minify --bundle --platform=node --outfile=dist/ReaMake.cjs",
    "build-pkg": "pkg ./dist/ReaMake.cjs --out-path=./build -t latest-win --compress GZip",
    "copy-assets": "copyfiles -u 1 -e **/*.js bin/* build",
    "build": "yarn build-cjs && yarn build-pkg && yarn copy-assets"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "adm-zip": "^0.5.10",
    "ansi-colors": "^4.1.3",
    "copy-paste": "^1.3.0",
    "nanoid": "^4.0.1",
    "watcher": "^2.2.2"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "esbuild": "^0.17.11",
    "pkg": "^5.8.0"
  }
}
